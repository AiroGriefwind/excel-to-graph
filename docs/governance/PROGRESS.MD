# PROGRESS

## 记录规则
- 每完成一个里程碑更新一次。
- 每条记录必须包含：日期、完成内容、验证方式、风险/阻塞、下一步。
- 如果计划调整，必须写明原因与影响范围。

## 进度模板
```md
### YYYY-MM-DD HH:mm
- 完成：
- 验证：
- 风险/阻塞：
- 下一步：
```

## 当前进度
### 2026-03-17 17:00
- 完成：初始化治理文档框架，确定分层结构（app/tabs/utils/tests）。
- 验证：目录检查通过；文档文件创建。
- 风险/阻塞：尚未落地 Python 依赖文件，需后续补齐运行说明。
- 下一步：实现 Excel 解析与筛选聚合工具函数。

### 2026-03-17 17:25
- 完成：实现 `src/utils/` 数据层（解析、清洗、筛选、聚合、预设持久化）。
- 验证：通过 `python -m compileall app.py src tests/smokes` 语法检查。
- 风险/阻塞：`date` 字段存在格式多样性，解析时会出现 pandas 推断警告。
- 下一步：完成 tabs 分区渲染与 app 薄入口整合。

### 2026-03-17 17:38
- 完成：实现 `src/tabs/` 五个区域渲染函数与 `app.py` 页面编排；新增 `requirements.txt`。
- 验证：页面流程可覆盖上传、筛选、预设保存、图表、卡片展示链路。
- 风险/阻塞：运行 UI 前需先安装依赖（`streamlit/pandas/openpyxl/plotly`）。
- 下一步：补齐 `tests/smokes` 冒烟脚本并执行验证。

### 2026-03-17 17:48
- 完成：新增并执行冒烟脚本 `tests/smokes/smoke_data_pipeline.py`、`tests/smokes/smoke_filter_presets.py`。
- 验证：两项脚本均通过；数据管道样本 4 份、解析总记录 177；预设读写/删除通过。
- 风险/阻塞：样本日期解析存在 warning（不影响结果），后续可按实际日期格式精细化解析。
- 下一步：继续功能迭代（可选：增加更多图表、导出功能、单元测试）。

### 2026-08-02 16:20
- 完成：新增 `src/utils/format_normalize.py`，在解析阶段对「形式」做业务归一化；保留 `format_raw`，筛选器用归一后的 `format`。
- 规则：`新聞報道（連視頻）` 并入 `新聞報道`；`評論文章/博客文章` 按语言归为 `評論/博客文章（中|英|其他）`；带连视频则归为 `評論/博客文章（連視頻）（中|英|其他）`；`BastilleGlobal` 强制英文。
- 验证：`smoke_format_normalize.py`、`smoke_data_pipeline.py` 通过。
- 风险/阻塞：现有 mock 样本尚无「连视频」评论/博客样例，相关分支靠单测覆盖；若线上「连视频」写法不同需再补规则。
- 下一步：按用户反馈继续跟进其他意见。

### 2026-08-02 16:30
- 完成：内容卡片改为展示 `format_raw`（原始形式）；筛选仍用归一化 `format`。解析阶段剔除形式为「總數/总数/合計」的统计行。
- 验证：`smoke_data_pipeline.py`、`smoke_format_normalize.py` 通过。
- 风险/阻塞：无。
- 下一步：继续跟进其他用户意见。

### 2026-08-02 16:35
- 完成：汇总并推送本轮用户反馈改动：
  1) 形式业务归一化（新闻合并、评论/博客中英与连视频分类）；
  2) 筛选看归一分类、卡片展示原始形式；
  3) 解析阶段剔除「總數」统计行；
  4) 新增 `smoke_format_normalize.py`，同步更新 `STRUCTURE.MD`。
- 验证：本地冒烟脚本通过；准备 push 到 `origin/main`。
- 风险/阻塞：历史保存的筛选预设若仍使用旧形式名称（如「評論文章」），需用户重新保存。
- 下一步：部署 Streamlit 线上版后做一次新 Excel 验收。

### 2026-08-02 17:15
- 完成：新增 Word 统计报告导出。`report_export.py` 生成固定分类汇总表；`export_tab.py` 提供表格预览与 `.docx` 下载；依赖增加 `python-docx`。
- 验证：`smoke_report_export.py`、`smoke_data_pipeline.py` 通过。
- 风险/阻塞：初期报表行未含「新聞報道」（后续已补）。
- 下一步：按用户反馈完善报表交互与文案。

### 2026-08-02 17:35
- 完成：完善并推送统计报告导出能力：
  1) 报表文案全面繁体；行含「新聞報道」置首，其后为評論/博文（中/英及連視頻）、專題、影片、貼文、總數；
  2) 列：分類 / 數目 / 瀏覽量 / 平均 / 互動量 / 平均；預覽改 HTML 表以支持重複「平均」列名；
  3) 勾選「受篩選器影響」切換全量/篩選數據，預覽與匯出同步生效（默認全量）；
  4) 總數行「平均互動量」不統計；
  5) 同步 `STRUCTURE.MD` / `TECHSTACK.MD` / `PRD.MD` / `requirements.txt`。
- 验证：`smoke_report_export.py` 通过。
- 风险/阻塞：无。
- 下一步：部署 Streamlit 线上版验收导出流程。

### 2026-08-02 17:45
- 完成：报表导出格式优化并推送：
  1) Word 分类列按最长标签加宽并禁换行（`noWrap`），确保「評論/博文（中）連視頻」等同行列名单行显示；
  2) 预览与导出数值统一千分位逗号（如 `8,818,979`；平均值保留一位小数）；
  3) 页面预览同步 `white-space: nowrap`。
- 验证：`smoke_report_export.py` 通过（含千分位断言）。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 后验收导出 Word 版式。

### 2026-08-02 17:50
- 完成：报表交互与对齐微调并推送：
  1) 预览与 Word 导出中，所有数字列改为右对齐（分类列仍左对齐）；
  2) 「受篩選器影響」勾选框默认改为开启，仍可手动关闭以统计全量数据。
- 验证：本地改动检查通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收默认筛选联动与右对齐效果。

### 2026-08-02 17:55
- 完成：Word 报表全列自适应宽度并推送：按每列最长内容（含千分位数字）估算列宽，全部单元格 `noWrap`，避免浏览量等长数字换行。
- 验证：`smoke_report_export.py` 通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收导出表格列宽。

### 2026-08-02 18:05
- 完成：报表副标题统一改为当前筛选条件的「开始日期 - 结束日期」（`YYYY/MM/DD`），预览与 Word 导出同步；若筛选未限定日期则回退为数据日期范围。
- 验证：本地改动检查通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收副标题日期格式。

### 2026-08-11 11:40
- 完成：折线图交互增强并推送。悬浮显示「标题 + 日期 + 瀏覽量/互動量」（标题取当日该指标最高内容）；折线高亮最高/最低节点。
- 验证：`smoke_data_pipeline.py` 通过。
- 风险/阻塞：无。
- 下一步：线上 Streamlit 验收折线图悬浮与高低值高亮。

### 2026-08-16 19:26
- 完成：导出区改为双 TAB：
  1) TAB1「分類統計表」为原有统计报告（逻辑不变）；
  2) TAB2「英文版博文表（BastilleGlobal）」为新增：仅列平台 BastilleGlobal 文章，列为 數目（順排編號）/日期（YYYYMMDD）/欄目（暫留空）/標題/瀏覽量（千分位），按日期升序；
  3) TAB2 同樣提供「受篩選器影響」勾選（默認開）、日期副標題、HTML 預覽與 Word 匯出（標題/欄目列可換行，數字列 noWrap、瀏覽量右對齊）。
- 新增 `src/utils/bastille_report.py`、`tests/smokes/smoke_bastille_report.py`。
- 验证：`smoke_bastille_report.py`（合成數據 + mock 樣本 50 篇）及其余 4 個冒煙腳本全部通过。
- 风险/阻塞：「欄目」Excel 暫無來源數據，目前固定留空；後續若 Excel 增加欄目/作者列需補解析映射。
- 下一步：线上 Redeploy 验收 TAB 切换与博文表导出版式。

### 2026-09-04 16:11
- 完成：项目与治理文档全面对齐复查，并落地两笔治理动作：
  1) 提交并推送 2026-08-16 的 Bastille 双 TAB 导出工作（此前仅存在于工作区，远端停在 `f7a1ada`）；
  2) 清理 git 对 `__pycache__/*.pyc`（18 个文件）的跟踪——`.gitignore` 规则早已存在，但初始提交先于规则，需 `git rm -r --cached` 才生效。
- 验证：完整阅读 6 份治理文档并逐项核对（STRUCTURE 与实际目录一致、TECHSTACK 与 `requirements.txt` 一致、PRD 必做 8 项全部落地）；`python -m compileall` 通过；5 个冒烟脚本全部通过（数据管道 173 条记录）。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收 TAB 切换与博文表导出版式（依赖本次推送完成）。

### 2026-09-04 17:05
- 完成：统计报表由「固定白名单」改为「锚定行 + 动态行」结构（用户反馈：新 Excel 含「專題報告」「專題文章」，报表只见「專題」且總數漏统计）：
  1) 锚定行固定顺序、永远显示，候选值改为集合以涵盖繁简写法（`帖文/貼文` 併入「貼文」，`專題報道/專題` 併入「專題」）；
  2) 锚定之外的形式自动单独成行（如 `專題報告`、`專題文章`、`直播`），按瀏覽量降序，仅在实际存在时出现；形式为空归入「未標註形式」；
  3) 總數改为统计全部数据，锚定行 + 动态行完整覆盖，分项加总恒等于總數，根治静默漏统计；
  4) 筛选器与归一化层零改动（專題報告/專題文章在筛选器中本就独立显示，符合用户「保持细分」的选择）；
  5) `export_tab.py` 提示文案同步（「僅統計固定分類」已不成立）。
- 验证：`smoke_report_export.py` 重构为锚定回归 + 动态行场景两组断言（含繁简合并、空形式、分项加总=總數）；5 个冒烟脚本全部通过；mock 数据报表输出与改动前逐值一致（向后兼容）。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收：上传含「專題報告」「專題文章」的新 Excel，核对报表动态行与總數。

### 2026-09-04 17:45
- 完成：移除报表「專題」锚定行，專題族全走动态行（线上验收反馈：报表仍显示「專題」「專題文章」，21 条被锚定行压扁为旧称「專題」）：
  1) 原锚定行 `(("專題報道","專題"), "專題")` 删除——按用户「保持细分」的定位，`專題報道`/`專題`/`專題報告`/`專題文章` 各自独立成行，行名=Excel 原始写法；
  2) 其余锚定行（新聞報道/評論博文四行/影片/貼文）保留，它们承担真实的业务合并与改名（如按语言归类、繁简併入）；
  3) 總數口径不变（全量）。
- 验证：`smoke_report_export.py` 两组断言同步更新（專題報道作为动态行的行名、顺序、数值）；5 个冒烟脚本全部通过；mock 回归：`專題報道` 以原名成行（37 条/176,768 瀏覽量），總數 173/2,779,595 不变。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收：确认报表显示「專題報道」（而非「專題」）与「專題文章」，數目与 Excel 原表一致。

### 2026-09-04 18:10
- 完成：解析「欄目」列并填充英文版博文表（用户反馈：新 Excel 的欄目数据存在但博文表显示「暫無數據」）：
  1) 新 Excel 表头为 8 列（`日期|平台|欄目|標題|形式|連結|瀏覽量|互動量`，較旧格式新增欄目且標題/形式换位），解析按列名识别故其余列无碍；`COLUMN_ALIASES` 增加 `欄目/栏目 -> section`，`STANDARD_COLUMNS` 纳入 `section`（旧文件无此列自动置空）；
  2) `excel_loader.py` 对 `section` 做文本清洗（strip + None/nan 噪音剔除）；
  3) `bastille_report.py` 博文表「欄目」改由 `section` 填充（`-` 原样保留、缺失留空）；`export_tab.py` 提示按实际数据切换（有欄目数据时不再显示「暫無數據」）。
- 验证：5 个冒烟脚本通过（`smoke_bastille_report.py` 增加 section 填充断言）；真实新文件 `國安專頁 Daily Report_260901.xlsx` 端到端：解析 409 行、BastilleGlobal 62 篇、欄目填充（Bastille Commentary/SD Advocates/Deep Throat 等）；报表專題報道 21/112,625、專題文章 5/14,980 与用户线上截图数字一致；mock 回归不变。
- 风险/阻塞：`section` 目前仅用于博文表；筛选器与卡片未暴露欄目维度（可作后续迭代）。
- 下一步：线上 Redeploy 验收：英文版博文表欄目列有值、Word 导出欄目列版式正常。

### 2026-09-04 18:20
- 完成：真正落地 `__pycache__` 清理，并更正 16:11 条目的失实记录：
  1) 复查发现 16:11 的 chore 提交（`6d4cdc9`）并未移除 pyc 跟踪——当时用 `git commit -- .` 的 pathspec 提交方式，把工作区仍存在的 pyc 重新提交了进去（该提交实际内容是 9 个 pyc 的内容更新），18 个 pyc 一直处于被跟踪状态；
  2) 本次重新 `git rm -r --cached` 全部 pycache，改用无 pathspec 的普通提交（仅提交暂存区的删除），`.gitignore` 规则就此生效；
  3) 经验：暂存了 `git rm --cached` 删除后，禁止用 pathspec 形式 `git commit -- .` 提交，会把工作区文件重新纳入。
- 验证：`git ls-files` 确认无任何 `.pyc` 被跟踪；`git status` 干净（pyc 改动不再出现）。
- 风险/阻塞：无。
- 下一步：无（治理修正）。

### 2026-09-04 18:45
- 完成：修复 Word 导出表格等宽问题，并调整英文版博文表列宽比例（用户反馈：博文表导出各列等宽，標題应最宽且自动换行）：
  1) 根因：python-docx 此前只写了每个单元格的 `tcW`，未重写表格级 `tblGrid` 网格列宽——Word 固定布局按建表时均分的 tblGrid 渲染，表现为等宽；
  2) `report_export.py` 新增 `_apply_table_grid()`（写入 tblGrid/tblW，1cm=567 twips），`build_report_docx` 与 `build_bastille_docx` 均接入——分類統計表的 Word 导出同样受此 bug 影响，一并修复；
  3) 博文表列宽调整为 `[1.2, 1.9, 2.6, 7.7, 2.1]` cm（合計 15.5cm，貼合 A4 預設邊距可用寬度）：標題 7.7cm 最宽且允许换行，欄目 2.6cm 可换行，數目/日期/瀏覽量 noWrap。
- 验证：5 个冒烟脚本通过，两个 docx 冒烟新增 tblGrid 断言（各列不等宽、博文表標題列最宽）；真实文件生成 docx 检查网格：博文表 `[1.2, 1.9, 2.6, 7.7, 2.1]`、統計表按内容自适应 `[5.17, 1.8, 3.35, 3.07, 2.51, 2.51]`。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收：博文表 Word 导出標題列最宽、长标题换行、其余列收窄；統計表 Word 列宽恢复按内容自适应。

### 2026-09-04 19:10
- 完成：博文表 Word 导出版式按用户反馈二次调整：
  1) 日期折行修复：1.9cm 列宽 + 默认 0.19cm/侧单元格内边距，内容区仅 1.52cm，容不下 YYYYMMDD（约 1.6cm）导致 dd 折到下一行（`noWrap` 在固定布局下不阻止折行）；日期列加宽至 2.1cm 并将单元格左右内边距收窄至 0.15cm/侧（`_apply_table_grid` 新增 `cell_margin_cm` 参数，写 tblCellMar），内容区 1.8cm 确保单行；
  2) 標題 7.7 -> 10.2cm（最宽、可换行），瀏覽量 2.1 -> 1.8cm，欄目维持 2.6cm；
  3) 表格整体加宽：两张导出 docx 页面左右边距从默认 2.54cm 收至 1.5cm（可用宽 15.9 -> 18cm，新增 `_set_narrow_page_margins`），博文表总宽 17.9cm。
- 验证：5 个冒烟脚本通过；博文表冒烟新增断言（日期列 ≥2.0cm 单行守卫、总宽 ≤18cm 页面可用宽）；真实文件 docx 网格实测 `[1.2, 2.1, 2.6, 10.2, 1.8]`。
- 风险/阻塞：統計表自适应列宽总和（真实数据 18.41cm）略超 18cm 可用宽，固定布局下向右边距溢出约 0.4cm，仍在 A4 页面内（1.5+18.41=19.91 < 21cm），暂不处理。
- 下一步：线上 Redeploy 验收：日期单行、標題列宽度与换行、整表加宽效果。

