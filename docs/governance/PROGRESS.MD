# PROGRESS

## 记录规则
- 每完成一个里程碑更新一次。
- 每条记录必须包含：日期、完成内容、验证方式、风险/阻塞、下一步。
- 如果计划调整，必须写明原因与影响范围。

## 进度模板
```md
### YYYY-MM-DD HH:mm
- 完成：
- 验证：
- 风险/阻塞：
- 下一步：
```

## 当前进度
### 2026-03-17 17:00
- 完成：初始化治理文档框架，确定分层结构（app/tabs/utils/tests）。
- 验证：目录检查通过；文档文件创建。
- 风险/阻塞：尚未落地 Python 依赖文件，需后续补齐运行说明。
- 下一步：实现 Excel 解析与筛选聚合工具函数。

### 2026-03-17 17:25
- 完成：实现 `src/utils/` 数据层（解析、清洗、筛选、聚合、预设持久化）。
- 验证：通过 `python -m compileall app.py src tests/smokes` 语法检查。
- 风险/阻塞：`date` 字段存在格式多样性，解析时会出现 pandas 推断警告。
- 下一步：完成 tabs 分区渲染与 app 薄入口整合。

### 2026-03-17 17:38
- 完成：实现 `src/tabs/` 五个区域渲染函数与 `app.py` 页面编排；新增 `requirements.txt`。
- 验证：页面流程可覆盖上传、筛选、预设保存、图表、卡片展示链路。
- 风险/阻塞：运行 UI 前需先安装依赖（`streamlit/pandas/openpyxl/plotly`）。
- 下一步：补齐 `tests/smokes` 冒烟脚本并执行验证。

### 2026-03-17 17:48
- 完成：新增并执行冒烟脚本 `tests/smokes/smoke_data_pipeline.py`、`tests/smokes/smoke_filter_presets.py`。
- 验证：两项脚本均通过；数据管道样本 4 份、解析总记录 177；预设读写/删除通过。
- 风险/阻塞：样本日期解析存在 warning（不影响结果），后续可按实际日期格式精细化解析。
- 下一步：继续功能迭代（可选：增加更多图表、导出功能、单元测试）。

### 2026-08-02 16:20
- 完成：新增 `src/utils/format_normalize.py`，在解析阶段对「形式」做业务归一化；保留 `format_raw`，筛选器用归一后的 `format`。
- 规则：`新聞報道（連視頻）` 并入 `新聞報道`；`評論文章/博客文章` 按语言归为 `評論/博客文章（中|英|其他）`；带连视频则归为 `評論/博客文章（連視頻）（中|英|其他）`；`BastilleGlobal` 强制英文。
- 验证：`smoke_format_normalize.py`、`smoke_data_pipeline.py` 通过。
- 风险/阻塞：现有 mock 样本尚无「连视频」评论/博客样例，相关分支靠单测覆盖；若线上「连视频」写法不同需再补规则。
- 下一步：按用户反馈继续跟进其他意见。

### 2026-08-02 16:30
- 完成：内容卡片改为展示 `format_raw`（原始形式）；筛选仍用归一化 `format`。解析阶段剔除形式为「總數/总数/合計」的统计行。
- 验证：`smoke_data_pipeline.py`、`smoke_format_normalize.py` 通过。
- 风险/阻塞：无。
- 下一步：继续跟进其他用户意见。

### 2026-08-02 16:35
- 完成：汇总并推送本轮用户反馈改动：
  1) 形式业务归一化（新闻合并、评论/博客中英与连视频分类）；
  2) 筛选看归一分类、卡片展示原始形式；
  3) 解析阶段剔除「總數」统计行；
  4) 新增 `smoke_format_normalize.py`，同步更新 `STRUCTURE.MD`。
- 验证：本地冒烟脚本通过；准备 push 到 `origin/main`。
- 风险/阻塞：历史保存的筛选预设若仍使用旧形式名称（如「評論文章」），需用户重新保存。
- 下一步：部署 Streamlit 线上版后做一次新 Excel 验收。

### 2026-08-02 17:15
- 完成：新增 Word 统计报告导出。`report_export.py` 生成固定分类汇总表；`export_tab.py` 提供表格预览与 `.docx` 下载；依赖增加 `python-docx`。
- 验证：`smoke_report_export.py`、`smoke_data_pipeline.py` 通过。
- 风险/阻塞：初期报表行未含「新聞報道」（后续已补）。
- 下一步：按用户反馈完善报表交互与文案。

### 2026-08-02 17:35
- 完成：完善并推送统计报告导出能力：
  1) 报表文案全面繁体；行含「新聞報道」置首，其后为評論/博文（中/英及連視頻）、專題、影片、貼文、總數；
  2) 列：分類 / 數目 / 瀏覽量 / 平均 / 互動量 / 平均；預覽改 HTML 表以支持重複「平均」列名；
  3) 勾選「受篩選器影響」切換全量/篩選數據，預覽與匯出同步生效（默認全量）；
  4) 總數行「平均互動量」不統計；
  5) 同步 `STRUCTURE.MD` / `TECHSTACK.MD` / `PRD.MD` / `requirements.txt`。
- 验证：`smoke_report_export.py` 通过。
- 风险/阻塞：无。
- 下一步：部署 Streamlit 线上版验收导出流程。

### 2026-08-02 17:45
- 完成：报表导出格式优化并推送：
  1) Word 分类列按最长标签加宽并禁换行（`noWrap`），确保「評論/博文（中）連視頻」等同行列名单行显示；
  2) 预览与导出数值统一千分位逗号（如 `8,818,979`；平均值保留一位小数）；
  3) 页面预览同步 `white-space: nowrap`。
- 验证：`smoke_report_export.py` 通过（含千分位断言）。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 后验收导出 Word 版式。

### 2026-08-02 17:50
- 完成：报表交互与对齐微调并推送：
  1) 预览与 Word 导出中，所有数字列改为右对齐（分类列仍左对齐）；
  2) 「受篩選器影響」勾选框默认改为开启，仍可手动关闭以统计全量数据。
- 验证：本地改动检查通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收默认筛选联动与右对齐效果。

### 2026-08-02 17:55
- 完成：Word 报表全列自适应宽度并推送：按每列最长内容（含千分位数字）估算列宽，全部单元格 `noWrap`，避免浏览量等长数字换行。
- 验证：`smoke_report_export.py` 通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收导出表格列宽。

### 2026-08-02 18:05
- 完成：报表副标题统一改为当前筛选条件的「开始日期 - 结束日期」（`YYYY/MM/DD`），预览与 Word 导出同步；若筛选未限定日期则回退为数据日期范围。
- 验证：本地改动检查通过。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收副标题日期格式。

### 2026-08-11 11:40
- 完成：折线图交互增强并推送。悬浮显示「标题 + 日期 + 瀏覽量/互動量」（标题取当日该指标最高内容）；折线高亮最高/最低节点。
- 验证：`smoke_data_pipeline.py` 通过。
- 风险/阻塞：无。
- 下一步：线上 Streamlit 验收折线图悬浮与高低值高亮。

### 2026-08-16 19:26
- 完成：导出区改为双 TAB：
  1) TAB1「分類統計表」为原有统计报告（逻辑不变）；
  2) TAB2「英文版博文表（BastilleGlobal）」为新增：仅列平台 BastilleGlobal 文章，列为 數目（順排編號）/日期（YYYYMMDD）/欄目（暫留空）/標題/瀏覽量（千分位），按日期升序；
  3) TAB2 同樣提供「受篩選器影響」勾選（默認開）、日期副標題、HTML 預覽與 Word 匯出（標題/欄目列可換行，數字列 noWrap、瀏覽量右對齊）。
- 新增 `src/utils/bastille_report.py`、`tests/smokes/smoke_bastille_report.py`。
- 验证：`smoke_bastille_report.py`（合成數據 + mock 樣本 50 篇）及其余 4 個冒煙腳本全部通过。
- 风险/阻塞：「欄目」Excel 暫無來源數據，目前固定留空；後續若 Excel 增加欄目/作者列需補解析映射。
- 下一步：线上 Redeploy 验收 TAB 切换与博文表导出版式。

### 2026-09-04 16:11
- 完成：项目与治理文档全面对齐复查，并落地两笔治理动作：
  1) 提交并推送 2026-08-16 的 Bastille 双 TAB 导出工作（此前仅存在于工作区，远端停在 `f7a1ada`）；
  2) 清理 git 对 `__pycache__/*.pyc`（18 个文件）的跟踪——`.gitignore` 规则早已存在，但初始提交先于规则，需 `git rm -r --cached` 才生效。
- 验证：完整阅读 6 份治理文档并逐项核对（STRUCTURE 与实际目录一致、TECHSTACK 与 `requirements.txt` 一致、PRD 必做 8 项全部落地）；`python -m compileall` 通过；5 个冒烟脚本全部通过（数据管道 173 条记录）。
- 风险/阻塞：无。
- 下一步：线上 Redeploy 验收 TAB 切换与博文表导出版式（依赖本次推送完成）。

